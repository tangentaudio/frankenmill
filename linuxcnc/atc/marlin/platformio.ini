; FrankenMill ATC — PlatformIO Configuration
;
; PLACEHOLDER — Replace this file with your actual platformio.ini
;
; This file specifies the board target and build options for the
; ATC Marlin firmware. Copy it to the Marlin source root before building.

[platformio]
; default_envs = YOUR_BOARD_ENV

[env:default]
; platform = ...
; board = ...
; framework = arduino
